---
name: change-review
description: 変更後の自己レビューを行い、必要な場合だけ review-quality / review-security を使って最終確認する。実装後の出口整理に使用する。
metadata:
  short-description: 変更レビュー
---

# Change Review

高品質なバイブコーディングのための、出口専用レビュースキル。
目的は、変更後に「何が問題か」「何が未確認か」を短く正確に整理すること。

## 前提

- [AGENTS.md](../../AGENTS.md) の検証と報告の基準を前提に適用する。
- 変更内容と検証結果が確認できることを前提にする。

## 目的

- 変更後の自己レビューを標準化する。
- review agent を使う条件を明確にする。
- 指摘がある場合は findings-first で返し、ない場合も残リスクを明示する。

## 対象

- 実装後の最終確認。
- 複数ファイル変更や境界変更を含む差分。
- 変更内容をユーザーへ返す前の品質整理。

## 対象外

- 実装前の設計レビュー。
- ドキュメントだけの軽微更新で、レビュー判断が不要な依頼。

## 基本方針

- まず自己レビューし、必要時のみ review agent を使う。
- 問題があれば findings-first で返す。
- 問題がなくても、未検証事項や残リスクは隠さない。

## review agent を使う条件

- 次のいずれかに当てはまる場合のみ `review-quality` または `review-security` を使う。
  - 境界変更がある
  - 複数ファイル変更で追跡が難しい
  - 認証認可、秘密情報、外部入出力に触れる
  - 変更影響が広い
- 単純変更では自己レビューのみで止めてよい。

## 出力ガイド

- 指摘がある場合:
  - 重大度順に findings を並べる
  - その後に open questions や残リスクを添える
- 指摘がない場合:
  - `重大な指摘なし` を明示する
  - 残リスク、未検証事項があれば続ける
