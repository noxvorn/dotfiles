prefix_rule(
  pattern = ["python", "-m", "pip", "install"],
  decision = "prompt",
  justification = "python -m pip による Python 依存追加は影響が大きいため都度確認する。",
  match = [
    "python -m pip install requests",
  ],
  not_match = [
    "python -m pip list",
  ],
)
