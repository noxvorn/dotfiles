prefix_rule(
  pattern = ["git", "log"],
  decision = "allow",
  justification = "commit range や履歴確認に必要な読み取り専用の `git log` を許可する。",
  match = [
    "git log @{u}..HEAD",
    "git log --oneline origin/main..HEAD",
  ],
)
