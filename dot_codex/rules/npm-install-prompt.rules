prefix_rule(
  pattern = ["npm", "install"],
  decision = "prompt",
  justification = "npm 依存追加は影響が大きいため都度確認する。",
  match = [
    "npm install",
    "npm install lodash",
  ],
  not_match = [
    "npm ci",
  ],
)
