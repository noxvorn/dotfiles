prefix_rule(
  pattern = ["yarn", "publish"],
  decision = "forbidden",
  justification = "package publish は Codex ではなくユーザーが実行する。",
  match = [
    "yarn publish",
    "yarn publish --access public",
  ],
  not_match = [
    "yarn pack",
    "yarn install",
  ],
)
