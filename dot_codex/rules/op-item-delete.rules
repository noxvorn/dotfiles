prefix_rule(
  pattern = ["op", "item", "delete"],
  decision = "forbidden",
  justification = "1Password item delete は Codex ではなくユーザーが実行する。",
  match = [
    "op item delete example",
  ],
  not_match = [
    "op item get example",
    "op item list",
  ],
)
