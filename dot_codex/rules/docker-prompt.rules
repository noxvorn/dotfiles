prefix_rule(
  pattern = ["docker"],
  decision = "prompt",
  justification = "Docker 操作は影響範囲が大きいため都度確認する。",
  match = [
    "docker ps",
    "docker build -t app .",
  ],
  not_match = [
    "docker --help",
  ],
)
