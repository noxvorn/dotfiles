prefix_rule(
  pattern = ["sudo"],
  decision = "prompt",
  justification = "権限昇格を伴う操作は影響が大きいため都度確認する。",
  match = [
    "sudo rm -rf /tmp/test",
    "sudo systemsetup -getcomputersleep",
    "sudo launchctl list",
  ],
)
