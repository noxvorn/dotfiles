prefix_rule(
  pattern = ["pnpm", "publish"],
  decision = "forbidden",
  justification = "package publish は Codex ではなくユーザーが実行する。",
  match = [
    "pnpm publish",
    "pnpm publish --access public",
  ],
  not_match = [
    "pnpm pack",
    "pnpm install",
  ],
)
