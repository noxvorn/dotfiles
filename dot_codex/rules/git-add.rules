prefix_rule(
  pattern = ["git", "add"],
  decision = "allow",
  justification = "`git-commit` skill の手順で使う `git add` を許可する。",
  match = [
    "git add README.md",
    "git add path/to/file",
    "git add dir/file1 dir/file2",
  ],
)
