prefix_rule(
  pattern = ["git", "add"],
  decision = "prompt",
  justification = "ステージ変更は影響範囲を誤りやすいため都度確認する。",
  match = [
    "git add .",
    "git add README.md",
    "git add --patch",
    "git add -p",
  ],
)
