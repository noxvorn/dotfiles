prefix_rule(
  pattern = ["git", "commit"],
  decision = "allow",
  justification = "通常の git commit 操作のみ許可する。",
  match = [
    "git commit -m \"chore: update\"",
    "git commit",
  ],
  not_match = [
    "git commit --amend",
    "git commit --amend --no-edit",
  ],
)
