prefix_rule(
  pattern = ["rm", "-f", "-r", "/"],
  decision = "forbidden",
  justification = "ルートファイルシステム削除は Codex から絶対に実行しない。",
  match = [
    "rm -f -r /",
  ],
  not_match = [
    "rm -f -r /tmp/example",
  ],
)
