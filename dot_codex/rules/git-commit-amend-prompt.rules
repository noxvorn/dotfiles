prefix_rule(
  pattern = ["git", "commit", "--amend"],
  decision = "prompt",
  justification = "amend は履歴を書き換えるため、明示依頼時のみ都度確認して扱う。",
  match = [
    "git commit --amend",
    "git commit --amend --no-edit",
  ],
)
