[project]
name = "chezmoi"
version = "0.1.0"
description = "Python execution environment for maintaining this chezmoi repo"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "pyyaml>=6.0.3",
]
